---
spring:
  config:
    activate:
      on-profile: tomcat-thread-100

server:
  tomcat:
    threads:
      max: 100

---
spring:
  config:
    activate:
      on-profile: tomcat-thread-20

server:
  tomcat:
    threads:
      max: 20

---
spring:
  config:
    activate:
      on-profile: tomcat-thread-1

server:
  tomcat:
    threads:
      max: 1
